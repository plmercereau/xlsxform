/**
 * Port of test_tutorial_xls.py - Test tutorial XLSForm.
 */

import { describe, it } from "vitest";

describe("TutorialTests", () => {
	it.todo("test_create_from_path - TODO: requires internal API (create_survey_from_path, tutorial.xls fixture)", () => {
		// The Python test loads a tutorial XLS fixture via create_survey_from_path.
		// This requires file-based survey creation and the tutorial.xls fixture.
	});
});
