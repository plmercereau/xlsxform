{
	"buildCommand": "pnpm --filter xlsxform run build && pnpm --filter @xlsxform/demo run build",
	"installCommand": "pnpm install --ignore-scripts",
	"outputDirectory": "demo/dist",
	"framework": "vite"
}
